{
  "scripts": {
    "install": "cd backend && npm install",
    "build": "cd frontend && npm run build",
    "start": "node backend/bin/www"
  },
  "devDependencies": {
    "tailwindcss": "^4.1.11"
  }
}
